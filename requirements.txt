# StudyHub — Python dependencies
#
# No requirements.txt/pyproject.toml/Pipfile was present in the
# supplied project, so this file is newly created as part of the
# background-jobs implementation (BACKGROUND_JOBS_IMPLEMENTATION.md
# §20's "Dependency file" row). It enumerates every third-party import
# already used across the existing codebase (grepped from every
# supplied file), plus the one new dependency this phase adds (rq).
#
# If your project already tracks dependencies elsewhere (a lockfile,
# a platform-specific manifest, etc.), reconcile this file against
# that source rather than treating this as authoritative — it was
# reverse-engineered from imports, not copied from an existing manifest.

Flask
Flask-SQLAlchemy
Flask-Mail
Flask-Migrate
Flask-Cors
Flask-Login
Flask-Limiter
Flask-Dance
Flask-SocketIO
simple-websocket
python-dotenv
SQLAlchemy
PyJWT
redis
apscheduler
bleach
eventlet
requests
Pillow
pandas
docx2txt
PyPDF2
cloudinary
firebase-admin
gunicorn

# New in this phase — background job queue (BACKGROUND_JOBS_IMPLEMENTATION.md §2)
rq
